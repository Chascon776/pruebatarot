import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tuempresa.tarotastral',
  appName: 'tarot-astral-app',
  webDir: 'dist'
};

export default config;
